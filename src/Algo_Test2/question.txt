实验二

实验目的与要求：理解分治法的基本思想和设计方法。

实验题目：

1．k-路合并操作问题：假定有k个有序数组，每个数组中含有n个元素，您的任务是将它们合并为单独的一个有序数组，该数组共有kn个元素。
   设计和实现 一个有效的分治算法解决k-路合并操作问题，并分析时间复杂度。

2. Split操作的要求是：以一个数组s和一个值v为输入，
    将数组s划分成3个子集：比v小的元素组成的集合，等于v的元素组成的集合以及比v大的元素组成的集合。
    设计和实现一种O(n)的就地split算法，即该算法不额外分配新的内存。
